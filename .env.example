# Banco de dados (SQLite por padrão para rodar sem infraestrutura externa)
DATABASE_URL="file:./dev.db"
# Para produção com Postgres, use algo como:
# DATABASE_URL="postgresql://user:password@host:5432/rh_saas"

# NextAuth
NEXTAUTH_SECRET="troque-por-um-valor-aleatorio-seguro"
NEXTAUTH_URL="http://localhost:3000"

# IA (opcional — se não configurado, o sistema usa um mock funcional)
ANTHROPIC_API_KEY=""
